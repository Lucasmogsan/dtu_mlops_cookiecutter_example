# import torch
# TODO: Not working as train has some references that doesn't work...


# from dtu_mlops_cookiecutter_example.train_model import train

# def test_train_improves_over_time():


#     # Train the model for a few epochs and record the loss
#     initial_loss = train(lr=0.001, epochs=1, batch_size=32, model_name="model")

#     # Train for a few more epochs
#     final_loss = train(lr=0.001, epochs=5, batch_size=32, model_name="model")

#     # Check that the loss has decreased
#     assert final_loss < initial_loss
